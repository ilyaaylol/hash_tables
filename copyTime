def copyTime(n, x, y):
    l = 0
    r = (n - 1) * max(x, y) #первый лист напечатан
    while l + 1 <= r:
        middle = (l + r) / 2
        if middle / x + middle / y < n - 1:
            l = middle
        else:
            r = middle
    return r + min(x, y)
print(copyTime(5, 2 , 1))
